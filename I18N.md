# 🌍 Internationalisierung (i18n)

Die Anwendung unterstützt mehrere Sprachen mit **next-intl**.

## 📋 Verfügbare Sprachen

- 🇬🇧 **Englisch (EN)** - Standardsprache
- 🇩🇪 **Deutsch (DE)**

## 🎯 Sprachauswahl

Die Sprache kann über den **Sprachwechsel-Button** in der Kopfzeile geändert werden:
- Klicken Sie auf **EN** für Englisch
- Klicken Sie auf **DE** für Deutsch

Die Auswahl wird in einem Cookie gespeichert und bleibt bei jedem Besuch erhalten.

## 📁 Struktur

```
4minitz-next/
├── i18n.ts                      # i18n Konfiguration
├── middleware.ts                # Locale-Handling
├── messages/
│   ├── en.json                  # Englische Übersetzungen
│   └── de.json                  # Deutsche Übersetzungen
└── components/
    └── LanguageSwitcher.tsx     # Sprachwechsel-Komponente
```

## 🔧 Verwendung in Komponenten

### Server Components

```tsx
import { useTranslations } from 'next-intl';

export default function MyComponent() {
  const t = useTranslations();
  
  return <h1>{t('home.title')}</h1>;
}
```

### Client Components

```tsx
'use client';

import { useTranslations } from 'next-intl';

export default function MyClientComponent() {
  const t = useTranslations();
  
  return <button>{t('common.save')}</button>;
}
```

## 📝 Übersetzungs-Keys

### Common (Allgemein)
```json
{
  "common.loading": "Loading..." / "Lädt...",
  "common.error": "Error" / "Fehler",
  "common.save": "Save" / "Speichern",
  "common.cancel": "Cancel" / "Abbrechen",
  "common.delete": "Delete" / "Löschen"
}
```

### Navigation
```json
{
  "nav.home": "Home" / "Startseite",
  "nav.meetingSeries": "Meeting Series" / "Besprechungsserien",
  "nav.minutes": "Minutes" / "Protokolle"
}
```

### Meeting Series
```json
{
  "meetingSeries.title": "Meeting Series" / "Besprechungsserien",
  "meetingSeries.createNew": "Create New..." / "Neue erstellen...",
  "meetingSeries.project": "Project" / "Projekt",
  "meetingSeries.participants": "Participants" / "Teilnehmer"
}
```

### Formulare
```json
{
  "meetingSeriesForm.projectLabel": "Project Name" / "Projektname",
  "meetingSeriesForm.nameLabel": "Series Name" / "Serienname",
  "meetingSeriesForm.submitCreate": "Create..." / "Erstellen..."
}
```

## ➕ Neue Übersetzung hinzufügen

### 1. Key in Sprachdateien hinzufügen

**messages/en.json**
```json
{
  "mySection": {
    "myKey": "My English text"
  }
}
```

**messages/de.json**
```json
{
  "mySection": {
    "myKey": "Mein deutscher Text"
  }
}
```

### 2. In Komponente verwenden

```tsx
const t = useTranslations();
return <p>{t('mySection.myKey')}</p>;
```

## 🆕 Neue Sprache hinzufügen

### 1. Sprachdatei erstellen

Erstellen Sie `messages/fr.json` für Französisch:
```json
{
  "common": {
    "loading": "Chargement...",
    "error": "Erreur"
  }
}
```

### 2. i18n.ts aktualisieren

```typescript
export const locales = ['en', 'de', 'fr'] as const;
```

### 3. LanguageSwitcher erweitern

```tsx
<button onClick={() => switchLanguage('fr')}>
  FR
</button>
```

## 🎨 Best Practices

### ✅ DO

```tsx
// Kurze, beschreibende Keys verwenden
t('meetingSeries.createNew')

// Verschachtelte Struktur für Organisation
t('errors.networkError')

// Parametrisierte Übersetzungen
t('meetingSeries.count', { count: 5 })
```

### ❌ DON'T

```tsx
// Keine langen Texte als Keys
t('This is a very long text that should be a key')

// Keine hartkodierten Texte
return <h1>Meeting Series</h1>  // ❌

// Richtig:
return <h1>{t('meetingSeries.title')}</h1>  // ✅
```

## 🔍 Debugging

### Fehlende Übersetzungen anzeigen

Wenn ein Key fehlt, wird der Key selbst angezeigt:
```tsx
t('missing.key')  // Ausgabe: "missing.key"
```

### Console Logs

```tsx
const t = useTranslations();
console.log(t('home.title')); // Zeigt die Übersetzung
```

### Browser-Tools

Die aktuelle Locale wird im Cookie `NEXT_LOCALE` gespeichert:
1. Öffnen Sie DevTools (F12)
2. Application → Cookies
3. Suchen Sie nach `NEXT_LOCALE`

## 🌐 Locale-Persistenz

Die Sprachauswahl wird automatisch gespeichert:
- **Cookie**: `NEXT_LOCALE` (1 Jahr Gültigkeit)
- **Pfad**: `/` (gilt für gesamte App)
- **Reload**: Seite wird neu geladen für sofortige Anwendung

## 📊 Übersetzungsfortschritt

| Bereich | EN | DE |
|---------|----|----|
| Common | ✅ | ✅ |
| Navigation | ✅ | ✅ |
| Home | ✅ | ✅ |
| Meeting Series | ✅ | ✅ |
| Formulare | ✅ | ✅ |
| Minutes | ✅ | ✅ |
| Topics | ✅ | ✅ |
| Labels | ✅ | ✅ |
| Errors | ✅ | ✅ |

## 🚀 Deployment

Bei Deployment müssen keine zusätzlichen Schritte unternommen werden:
- Alle Sprachdateien werden automatisch gebundelt
- Next.js optimiert die Übersetzungen automatisch
- Keine separaten Builds pro Sprache nötig

## 🔗 Weitere Ressourcen

- [next-intl Dokumentation](https://next-intl-docs.vercel.app/)
- [Next.js i18n Guide](https://nextjs.org/docs/app/building-your-application/routing/internationalization)
- [ICU Message Format](https://formatjs.io/docs/core-concepts/icu-syntax/)
